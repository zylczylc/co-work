journalctl -b -3 | sed 's/^[ ]*//g' | awk '!seen[$0]++'获取之前三次启动的启动日志，从启动日志中过滤掉每次都不相同的部分-b表示查看前三次启动的日志，-3表示显示前三次 journalctl -b -3 | sed 's/^[ ]*//g' | awk '!seen[$0]++' | sort | uniq -c重复记录输入行并对其计数,sed 's/^[ ]*//g'用于去除前面的空格。awk...用于过滤出不重复的行
journalctl -b -3 | sed 's/^[ ]*//g' | awk '!seen[$0]++' | sort | uniq -c | awk '$1 != 3'删除所有出现过三次的内容，因为这是重复部分，awk '$1 != 3' 用于只显示重复次数不等于3的行，即删除所有出现过三次的内容。

